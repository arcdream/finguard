export const response = {
    message: "",
    timestamp: new Date(),
    status: "failure"
  };

  export function createBackendResponse(responseMessage, status) {

    const newResponse = {
      ...response, 
      message: responseMessage,
      timestamp: new Date(),
      status: status
    };

    return newResponse;
  }


